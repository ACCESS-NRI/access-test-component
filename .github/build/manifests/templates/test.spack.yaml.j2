# This is a {{ adjective }} spack manifest file.
spack:
  specs:
  - access-test-component@git.{{ pr }} %intel@2021.10.0 target=x86_64
  packages:
    all:
      require:
      - '%intel@2021.10.0'
      - target=x86_64
view: true
concretizer:
  unify: false